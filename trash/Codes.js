
/*
let result [];
let max = 0;


for (car of cars) {
  if (cars.models > max) {
    max = b.models.length;
    result = [];
    result.push(b);
  } else if {
    result.push(b);
  }
}

if (result.length === 1) {
  return result[0].brand;
} else {
  return result.map((b) => b.brand)
}
-----------------------------------------------------------------------
let result = [];
let min = cars[0].models.length;
result.push(cars[0]);
          
for (let i = 1; i < cars.length; i++) {
  const car = cars[i];
  if (car.models.length < min) {
    min = car.models.length;
    result = [];
    result.push(car);
  } else if (car.models.lenght === min) {
    result.push(car);
  }
}

if (result.length === 1) {
  return result[0].brand;
} else {
  return result.map((b) => b.brand)
}

*/